# Streamlit Community Cloud — demo mode only
# Heavy packages (chromadb, sentence-transformers, torch) excluded
# They use lazy imports and are not called in demo_mode: true

streamlit>=1.35.0
plotly>=5.9.0
anthropic>=0.28.0
tenacity>=8.2.0
rank-bm25>=0.2.2
pandas>=2.0.0
pyyaml>=6.0
reportlab>=4.0.0
